{
  "name": "surf-app",
  "main": "server.js",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.16.2",
    "puppeteer": "^1.0.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.1",
    "react-scripts": "^1.0.17",
    "request": "^2.83.0"
  },
  "scripts": {
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "dev": "concurrently --kill-others \"react-scripts start\" \"npm run start\"",
    "start": "node server.js"
  }
}
